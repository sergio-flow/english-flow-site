import { texts as filterPhrasesTexts } from '../_components/templates/FilterPhrases';
import { texts as heroTexts } from '../_components/templates/Hero';
import { texts as navbarTexts } from '../_components/templates/Navbar';

const allTexts = {
    navbarTexts,
    heroTexts,
    filterPhrasesTexts,
};

export default allTexts;